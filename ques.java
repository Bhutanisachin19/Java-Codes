class Ques
{
    public static void  main(String[] args)
    {
        String x = "xyz";
        x.toUpperCase(); // if we store it in a varibale the it is changed to upper
        
        System.out.println(x);
        /*
        String y = x.replace('Y', 'y');
        y = y + "abc";
        System.out.println(y);
        */
    }
}